# ========== Telegram Parser ==========
# Required
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# Required for first sign-in (code is requested interactively)
TELEGRAM_PHONE=+70000000000

# Optional: if 2FA enabled, parser will use this password (or ask interactively)
TELEGRAM_2FA_PASSWORD=your_2fa_password

# ========== PostgreSQL (Pipeline) ==========
# Строка подключения к БД Pipeline (для WP sync и при необходимости для MCP).
# WP sync использует WP_DATABASE_URL или, если не задан, DATABASE_URL.
# Для MCP в Cursor задаётся отдельно в .cursor/mcp.json (скопировать из .cursor/mcp.json.example).
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/Pipeline
# Альтернативное имя для WP sync (если хотите разделить БД)
# WP_DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/Pipeline

# ========== WordPress Source (sync) ==========
# Для каждого сайта из config/wp-sites.yml задайте пару переменных.
# Имя: WP_SITE_<site_id>_USER и WP_SITE_<site_id>_APP_PASSWORD (site_id в верхнем регистре, дефис → подчёркивание).
# Пример для site_id: main
WP_SITE_MAIN_USER=wp_admin_login
WP_SITE_MAIN_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Пример для второго сайта (site_id: blog)
# WP_SITE_BLOG_USER=admin
# WP_SITE_BLOG_APP_PASSWORD=yyyy yyyy yyyy yyyy
